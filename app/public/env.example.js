// Copy this file to `env.js` (same folder) and fill in your Supabase project's
// values. `env.js` is gitignored — never commit real values here.
//
// The anon key is safe to ship to the browser: Supabase Row Level Security
// (see /supabase/schema.sql) is what actually protects your data, not this key.
window.__env = {
  SUPABASE_URL: 'https://your-project.supabase.co',
  SUPABASE_ANON_KEY: 'your-anon-key',
};
